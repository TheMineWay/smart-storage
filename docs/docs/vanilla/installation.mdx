---
sidebar_position: 2
---

# Installation

import InstallCommand from '@site/src/components/commands/install/install-command';

Use the following command to install the package to your project. You can customize the installation command to use another package manager and to target a specific package version.

<InstallCommand library="smart-storage"/>
